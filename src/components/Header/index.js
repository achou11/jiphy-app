import * as React from 'react'

import './Header.css'

const Header = ({ bookmarks, addBookmark, removeBookmark }) => (
  <header className='Header'>
    <h1>Jiphy</h1>
  </header>
)

export { Header }
